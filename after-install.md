# Feishu插件 Hermes特供版本已安装

可用能力：

- 飞书任务口径注入：少废话、先枚举、再操作
- Plugin skills：
  - `feishu-workbench:feishu-auth-doctor`
  - `feishu-workbench:feishu-chatops`
  - `feishu-workbench:feishu-office`

下一步：

1. 重启 Hermes gateway / CLI。
2. 发送 `/plugins` 确认 `feishu-workbench` 已加载。
3. 飞书授权异常时优先用 `/feishu auth`、`/feishu diagnose`、`/feishu doctor`。

